# План работ по созвону 20.07 — атомарные тикеты, гейты, верификация

**Кому:** Рита (и её Claude). **От:** Ник (Solomon Estate dev).
**Формат:** каждый тикет — атомарный, с машинной проверкой и скриншот-доказательством.
Ничего не принимаем «на глаз»: есть скрипт → гоняем скрипт; нет скрипта → скриншот.

---

## 0. Как этим пользоваться (для Claude)

1. Прочитай этот файл целиком **до** первой правки.
2. Работай **по одному тикету**. Не начинай следующий, пока текущий не прошёл свою проверку.
3. Между фазами — **ГЕЙТ**. Гейт не прошёл → чинишь, не идёшь дальше.
4. Все скрипты лежат в `tools/`, запускаются из корня репо:
   ```
   python tools/test_pages.py <dir>     # тест-гейт: SEO-контракт + смысл (46 проверок/стр)
   python tools/qa-lint.py A|B          # A = типографика, B = смысл/жаргон (должно быть 0)
   python tools/map-audit.py deploy     # карты: адрес ↔ координаты, ловит расхождения
   python tools/rita-patch.py <dir>     # лексика/гео сквозняком (идемпотентно, --dry для примерки)
   ```
5. Скриншоты — обязательная часть Definition of Done. Складывай в `_qa-shots/<тикет>/`.

## 1. Железные правила (нарушение = откат)

**Правим только ВИДИМОЕ.** Не трогаем: `<title>`, `<h1>`, `meta description`, JSON-LD (`schema`),
`canonical`, URL/слаги, внутренние ссылки, сущности в теле (имена зданий/улиц/архитекторов, числа).
Там весь SEO-вес, за который заплачено.

- **M ≥ N:** после правки сущностей/чисел/FAQ не меньше, чем до. Текст не вырезаем — **переплавляем**
  (в аккордеон, таблицу, колонки), но оставляем в DOM.
- **Кикеры/чипы/заголовки карточек = только позитивные факты.** Никаких гео-оговорок («не ЦАО»),
  англицизмов (off-market, adjacent, landmark, HNWI, due diligence) и SEO-жаргона (pillar, YMYL).
- **Дизайн-канон:** навы `#272F42`, терракота `#BB5C3C`, Anticva капсом в заголовках, квадратные углы,
  без теней, motion ≤ 2/5.
- Спорное (что резать / что оставить по SEO) — **не решаем на глаз**, пишем Нику.

---

## 2. Текущее состояние (проверено 20.07, факты)

**Что Рита уже сделала** (не переделывать):
- ✅ Подвал кликабельный + плашка «о себе» ушла с лица (`c2c38b7`)
- ✅ «Сплюснутая Украина» → квадратный полароид (`f71b7fc`)
- ✅ Счётчик хаба авангарда 13 → 11 (`92f74c7`)
- ✅ Фото Жолтовского (Моховая 13) и Дома академиков — настоящие
- ✅ 9 страниц раздела «Сталинки»

**Что ждёт в ветке `seo-qa-canon-fixes`** (запушена, НЕ смёржена в main):
- гео-фиксы: МГУ-кикер «ЮЗАО · не ЦАО» → «Воробьёвы горы» (296 замен по сайту)
- хаб высоток 9 → 7 («Дом на набережной» — не высотка, уезжает в Модернизм)
- 21 страница: 8 районов ЦАО, 3 высотки (Кудринская / Красные ворота / МИД), 10 авангарда
- фикс битых ссылок `/znakovye-doma/` → `/modernizm/`

**Что нашёл машинный аудит карт** (`tools/map-audit.py deploy`) — **10 CRIT**, это и есть
«карты врут» из созвона:
```
cao                      центр (55.746,37.601)   ≠ метка (55.7308,37.5905)   ~1.7 км
cao/khamovniki           центр ≠ метка
cao/tagansky             центр ≠ метка
cao/tverskoy             центр ≠ метка
cao/yakimanka            центр ≠ метка
modernizm/dom-mosselproma   schema (55.7529,37.5975) ≠ карта (55.7565,37.6006)   ← пример Володи
modernizm/dom-narkomfina    schema ≠ карта
modernizm/dom-zholtovskogo  центр ≠ метка И schema ≠ карта
visotki/kotelnicheskaya     schema ≠ карта
+ у Кудринской и Красных ворот карты нет вообще
```
**Диагноз:** координаты сами по себе правдоподобные, но **три источника спорят** — `data-center`
(куда смотрит карта), `data-points` (где метка), `schema geo` (что читает Яндекс). Пользователь видит
«точка не там». Чиним синхронизацией, а не переписыванием координат.

---

# ФАЗА 1 — Влить готовое (полдня)

### T-1.1 · Смёржить `seo-qa-canon-fixes`
**Что:** влить ветку в main. Она сделана НА твою версию, твои коммиты (мобилка, Котельническая,
список «Семь сестёр») targeted-правками обойдены — конфликтов быть не должно.
**Как:** `git merge origin/seo-qa-canon-fixes` → при конфликте: твоя версия вёрстки + наш текст.
**Проверка:**
- `python tools/qa-lint.py B` → **0 находок**
- `python tools/test_pages.py site/src/data` → **0 CRIT**
- `grep -c "ЮЗАО · не" site/src/data/visotki--mgu.html` → **0**
- `npm run build` → без ошибок, страниц стало +21
**Скриншот:** `/visotki/mgu/` (кикер = «Самая высокая из семи сестёр · Воробьёвы горы»), `/visotki/` (семь).

### T-1.2 · Прошить 21 новую страницу в навигацию
**Что:** новые страницы физически есть, но на них надо попасть кликом (Володя: «глаз режет, что одни
кликабельные, другие нет»).
- хаб `/visotki/`: карточки Кудринской, Красных ворот, МИД — раскликать (убрать «Страница готовится»)
- хаб `/cao/`: 8 новых районов в список/слайдер
- подвал + мегаменю: добавить новые адреса
**Проверка:** с главной за ≤ 2 клика доходишь до любой из 21 страницы; 0 ссылок-404
(`python tools/test_pages.py` секция внутренних ссылок).
**Скриншот:** хаб высоток и хаб ЦАО целиком.

> ### 🚦 ГЕЙТ G1 — не идти дальше, пока:
> `qa-lint B = 0` · `test_pages CRIT = 0` · `npm run build` зелёный · 21 страница кликабельна ·
> скриншоты хабов в `_qa-shots/G1/`.

---

# ФАЗА 2 — Авангард: +7 зданий (главный блок)

Володя проговорил поимённо. В слайдере сейчас 7 плиток, часть — не авангард (Моховая, Перцова, ЦДЛ);
в кнопке «11 адресов». Оставляем текущие 7 (**решено: они нужны по SEO**) и **добавляем 7 настоящих**,
чтобы счётчик стал честным.

| Тикет | Здание | Адрес (проверить!) | Заметка Володи |
|---|---|---|---|
| T-2.1 | **Дом Моссельпрома** | Калашный пер., 2/10 | уже есть страница — только вытащить в слайдер |
| T-2.2 | **Дом Мельникова** | Кривоарбатский пер., 10 | икона авангарда, обязательно |
| T-2.3 | **Здание Наркомзема** | Садовая-Спасская, 11/1 (Щусев) | |
| T-2.4 | **Здание Центросоюза** | Мясницкая, 39 (Ле Корбюзье) | |
| T-2.5 | **ДК имени Зуева** | Лесная, 18 (Голосов) | «в красной версии только» |
| T-2.6 | **Жилой дом артистов МХАТ** | Брюсов пер., 17 | |
| T-2.7 | **Дом общества «Динамо»** | Большая Лубянка, 5 | есть красивая **ночная** фотография — взять её |

**Для каждого тикета (шаблон, не отклоняться):**
1. Страница по образцу существующей авангард-страницы (тип геро `--building`), слаг в стиле раздела.
2. Контент: история · архитектор · стиль · охранный статус · **типология квартир / рынок вокруг**
   (если жилого фонда нет — честно «жилого фонда нет, это ориентир и рынок вокруг», как на МГУ).
3. `title` / `description` / `canonical` / JSON-LD (`Place` + `FAQPage`) — по контракту.
4. **Карта: `data-center` == `data-points[0].c` == `schema geo`** (см. Фазу 3, не создавай новый долг).
5. Карточка в слайдере хаба + ссылка из подвала.
6. Внутренние ссылки: на хаб + на 2 соседних здания.

**Проверка на каждый тикет:**
- `python tools/test_pages.py site/src/data` → новая страница **CRIT 9/9**
- `python tools/map-audit.py deploy` → по ней **0 CRIT**
- `python tools/qa-lint.py B` → 0
**Скриншот:** геро + одна секция тела + карта (видно, что метка на здании).

### T-2.8 · Согласовать счётчики
Кнопка «11 адресов», текст «13», в хабе «8 адресов» — привести к **фактическому числу карточек**.
**Проверка:** `grep -oE "[0-9]+&nbsp;адрес[а-я]*" site/src/data/modernizm.html` → одно и то же число,
равное количеству карточек в слайдере.

### T-2.9 · Стартовая картинка раздела «Авангард»
Сейчас там **высотка** — не по теме (Володя: «это не авангард Москвы»). Заменить на авангардное здание.
**Блокер:** конкретное фото выбирает Володя. До его ответа — поставить **Дом Мельникова** или
**Наркомфин** из имеющихся, чтобы не висела высотка.

> ### 🚦 ГЕЙТ G2:
> 7 новых страниц: `test_pages` 9/9 каждая · `map-audit` 0 CRIT · счётчики сходятся ·
> в слайдере ≥ 11 карточек · скриншоты всех 7 гер в `_qa-shots/G2/`.

---

# ФАЗА 3 — Карты (машинно доказанный баг)

### T-3.1 · Синхронизировать три источника координат
На каждой странице со зданием: `data-center` == `data-points[0].c` == `schema geo` (`latitude`/`longitude`).
Эталон — **адрес страницы**: находим здание в Яндекс.Картах, берём координаты **дома**, а не улицы.
**Список на починку — 10 CRIT из `tools/map-audit.py`** (см. раздел 2 выше).
**Проверка:** `python tools/map-audit.py deploy` → **CRIT = 0**.

### T-3.2 · Добавить карты туда, где их нет
Кудринская, Красные ворота (WARN «есть адрес, но карты нет») + все новые из Фазы 2.
**Проверка:** `map-audit` не показывает WARN «есть адрес, но карты нет».

### T-3.3 · Ручная сверка (машина не проверит)
`python tools/map-audit.py deploy --csv > maps.csv` → пройти таблицей, открыть каждую пару
адрес ↔ координаты в Яндекс.Картах, убедиться, что **метка стоит на здании**.
**Проверка:** скриншот карты для 5 контрольных: Моссельпром, Наркомфин, Жолтовский, Котельническая, ЦАО-хаб.

> ### 🚦 ГЕЙТ G3: `map-audit` CRIT = 0, WARN только по подписям меток (косметика) ·
> 5 контрольных скриншотов карт в `_qa-shots/G3/`.

---

# ФАЗА 4 — Навигация, цифры, ЦАО

### T-4.1 · «Районы» вместо «ЦАО» в меню и подвале
**Проверка:** в шапке/подвале нет пункта «ЦАО»; ссылки ведут на `/cao/`; скриншот шапки + подвала.

### T-4.2 · Подвал на заглавных страницах
Володя: «на внутренних кликабельные, а на заглавных нет». Ты чинила (`c2c38b7`) — **перепроверить
именно ХАБЫ**: `/visotki/`, `/cao/`, `/stalinki/`, `/modernizm/`, `/uslugi/`.
**Проверка:** на каждом хабе заголовки разделов в подвале — `<a>` с рабочим href; 0 битых.

### T-4.3 · Картинка раздела ЦАО
Сейчас та же, что у «Сталинок». Володя предложил: **стилизованная карта ЦАО / вид с воздуха** на округ
из 10 районов. Сгенерировать в бренд-палитре (навы + терракота, без пестроты).
**Проверка:** скриншот геро `/cao/` и `/stalinki/` рядом — картинки разные.

> ### 🚦 ГЕЙТ G4: `qa-lint A и B = 0` · `test_pages` 0 CRIT по всем страницам ·
> скриншоты шапки, подвала, гер ЦАО и Сталинок.

---

# ФАЗА 5 — Фото-пул (после чт–пт)

Придёт **~706 исходников со 2-й фотосессии (12 зданий)** + 1-я фотосессия уже на Яндекс.Диске.
Володя: «там будет из чего выбрать, больше ничего и не надо». **До получения пула массовую замену не начинать.**

- T-5.1 «Один человек на каждой сделке» — заменить повторяющееся фото
- T-5.2 Убрать старое фото (где «5000 в кармане»)
- T-5.3 `/uslugi/` — добавить фото Володи (сейчас он только на «о себе»)
- T-5.4 Премиум-вторичка ЦАО на главной — версия «Володя на фоне МГУ»
- T-5.5 Ряд фотографий из одной фотосессии — разбавить
- T-5.6 «О себе» — **апскейл** фото (низкое качество) + сдвиг вправо (проверить на десктопе и в
  мобильном «десктопном виде» — Володя смотрел так)
- T-5.7 Авангард — стартовая картинка по выбору Володи (закрывает T-2.9)

**Проверка каждого:** скриншот до/после + прогон `test_pages` (alt не потеряны, разметка цела).

---

# 6. Скриншот-протокол (обязателен)

Единый способ, чтобы скриншоты были сравнимы:
- Ширина **1440** (десктоп) и **390** (мобилка) — каждый визуальный тикет в обеих.
- Снимать: **геро целиком** + **изменённую секцию** + (если менялась) **карта**.
- Класть в `_qa-shots/<гейт>/<тикет>-<desktop|mobile>.png`.
- В комментарии к коммиту — что на скриншоте видно.

**Чек визуальной консистентности** (глазами, по каждой новой странице):
геро не пустой · шапка/подвал как на старых · нет горизонтальной прокрутки · нет текста, наезжающего
на фото · капсы заголовков на месте · терракота только на CTA.

# 7. Финальная приёмка (перед показом Володе)

```
python tools/test_pages.py site/src/data     → CRIT 0
python tools/qa-lint.py A                    → 0
python tools/qa-lint.py B                    → 0
python tools/map-audit.py deploy             → CRIT 0
npm run build                                → без ошибок
```
+ пройти кликом: главная → каждый хаб → 3 случайные новые страницы → форма (0 битых ссылок).
+ `_qa-shots/` укомплектован по всем гейтам.

**Известные блокеры (не наша вина, не блокируют остальное):**
фото-пул (чт–пт) · выбор картинки Авангарда (Володя) · QR-коды (ссылки Володи) ·
«РГР Топ-4 из 1080» — цифра не подтверждается в реестре ГРМ, ждём решения Володи (рекомендация —
заменить на «квалификация Эксперт», это подтверждено).

---

**Вопросы / спорное — Нику, не решать на глаз.** Особенно: что резать по SEO, состав слайдера
авангарда, формулировки регалий.
